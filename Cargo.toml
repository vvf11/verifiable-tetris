[workspace]
members = ["verifier", "example/fibonacci-program", "example/script"]

resolver = "2"

[workspace.dependencies]
sp1-verifier = { version = "0.4.0", default-features = false }
sp1-zkvm = { version = "0.4.0" }
sp1-build = { version = "0.4.0" }
